# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SSCustomButton' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
	pod 'SSCustomTabbar'
  # Pods for SSCustomButton

  target 'SSCustomButtonTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SSCustomButtonUITests' do
    # Pods for testing
  end

end
